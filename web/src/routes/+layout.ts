// Disable SSR for pure SPA mode with adapter-static fallback.
// This ensures all routes work correctly when refreshed directly in the browser.
export const ssr = false;
export const prerender = false;

